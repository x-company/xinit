version: "3.7"

services:
  xinit:
    image: xinit:unit
    build:
      context: ../../../
      target: xnodeimage
    volumes:
      - ../../../:/code/
    command: node node_modules/.bin/_mocha --require ts-node/register './tests/unit/xinit/**/*.Tests.ts'
