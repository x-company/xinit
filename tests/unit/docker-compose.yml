version: "3.7"

services:
  xinit:
    image: xinit:unit
    build:
      context: ../../
      target: dev
    volumes:
      - ../../:/code/
    command: node /usr/bin/mocha --require ts-node/register './tests/unit/**/*.Tests.ts'
