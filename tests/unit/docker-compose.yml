version: "3.7"

services:
  xinit:
    image: xinit:unit
    build:
      context: ../../.devcontainer
    volumes:
      - ../../:/workspace/
    command: node /usr/bin/mocha --require ts-node/register '/workspace/tests/unit/**/*.Tests.ts'
