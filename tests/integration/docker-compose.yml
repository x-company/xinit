version: "3.7"

services:
  xinit:
    image: xinit:integration
    build:
      context: ../../
      target: dev
    volumes:
      - ../../:/code/
    ports:
      - 3000:3000
      - 9229:9229
    command: nodemon --inspect=0.0.0.0:9229 --nolazy ./tests/integration/index.ts
